<!--
Append this section to the AGENTS.md of a Magento project using the migration
skill, then set MIGRATION_SKILLS_PATH to the skill directory relative to the
project root or to an absolute path.

Agents with native SKILL.md support do not need this snippet; they discover the
skill from its frontmatter description.
-->

## ScandiPWA-to-Hyvä migration skill

When migrating or verifying a ScandiPWA component, capability, slice, or complete
storefront program, read and follow
`MIGRATION_SKILLS_PATH/migrate-scandipwa-to-hyva/SKILL.md` completely.

Set `MIGRATION_SKILLS_PATH` = `<MIGRATION_SKILLS_PATH>` for this project's layout.

This routing instruction does not expand migration scope, authorize cutover or
deployment, or replace the project's approved rebuild/adapt/omit decisions.
